const API_URL = import.meta.env.VITE_API_URL;
const SURAH_URL = `${API_URL}/surah`;
const JUZ_URL = `${API_URL}/juz`;
const PAGE_URL = `${API_URL}/halaman`;

export { API_URL, SURAH_URL, JUZ_URL, PAGE_URL };
